import { CountStopsPipe } from './count-stops.pipe';

describe('CountStopsPipe', () => {
  it('create an instance', () => {
    const pipe = new CountStopsPipe();
    expect(pipe).toBeTruthy();
  });
});
